<div class="row">
  <h2>Your Games</h2>
  <br>
  <ul>
    <% @games.each_with_index do |game, index| %>
      <li>
        <b>Game <%= "#{index+1}" %></b> | <%= game.capacity %> Players | <%= game.game_status %>
      </li>
    <% end %>
  </ul>
  <br>
</div>
